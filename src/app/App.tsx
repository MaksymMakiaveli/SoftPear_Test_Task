import { LayoutHeader } from '../widgets/layout-header';
import { Container } from '@entities/layout';

export const App = () => {
  return (
    <Container>
      <LayoutHeader />
    </Container>
  );
};
